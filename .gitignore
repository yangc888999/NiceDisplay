# 编译产物（源码仓库不包含二进制）
*.app/
dlite
mkicon
dmg_stage/

# 安装包
#   根目录下的是 build.sh 的构建产物，不入库；
#   dist/ 下的是正式发布包，需要入库（本机网络无法用 GitHub Release 附件通道，
#   uploads.github.com 被拦，所以预编译包直接放进仓库分发）
/NiceDisplay-1.0.dmg
/dlite-1.0.dmg

# macOS 杂项
.DS_Store
._*
.Spotlight-V100
.Trashes

# 编辑器
*.swp
*~
.idea/
.vscode/

# 本地配置（含个人显示器 id / 偏好，不应提交）
*.conf
!*.conf.example
